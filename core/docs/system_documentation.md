# مستندات عملکرد سامانه مدیریت مشتریان

آخرین به‌روزرسانی: 1405/06/02  
مبنای مستند: وضعیت فعلی کدهای `payments/models.py`، `payments/views.py`، `payments/forms.py` و مسیرهای `payments/urls.py`

## 1. هدف سامانه

این سامانه برای مدیریت ارتباط عملیاتی شرکت با مشتریان، کارکنان واحدهای مالی، بازرگانی، فروش، گارانتی، مدیران و طرف‌حساب‌ها طراحی شده است. محور اصلی سیستم ثبت و پیگیری فیش‌های واریزی مشتریان است، اما امکانات مکملی مانند برنامه واریز، اطلاعیه روزانه فیش‌ها، فاکتورها، لیست قیمت، سفارش‌ها، پیش‌فاکتور، مغایرت‌گیری، مدیریت کاربران، دسترسی‌ها، گارانتی، درخواست نمایندگی، پیامک، PWA و خروجی اکسل نیز در سیستم وجود دارد.

اصول کلی سیستم:

- مشتری بتواند از وب یا محیط موبایلی/PWA فیش، سفارش، فاکتور و سایر اطلاعات خود را مشاهده و ثبت کند.
- کارکنان مالی، بازرگانی و فروش بتوانند اسناد را بر اساس نقش خود بررسی، تکمیل، ثبت، عودت، رد یا پیگیری کنند.
- داشبوردهای عملیاتی تا حد امکان فقط اسناد نیازمند اقدام را نشان دهند و اسناد انجام‌شده به سوابق منتقل شوند.
- تمام تغییرات مهم باید تاریخچه و نوتیفیکیشن داشته باشند.
- دسترسی هر کاربر بر اساس نقش، سطح دسترسی، مشتریان تخصیص‌یافته و ارتباط با سند کنترل شود.
- فایل‌های ضمیمه شامل تصویر و PDF باید در وب و موبایل قابل مشاهده و دانلود باشند.

## 2. نقش‌های کاربری

نقش‌ها در پروفایل کاربر و گاهی از طریق `is_staff` و `is_superuser` تعیین می‌شوند.

### نقش‌های اصلی

- `customer`: مشتری. دسترسی به پرتال مشتری، فیش‌های خود، برنامه واریز، سفارش‌ها، فاکتورها، لیست قیمت‌ها، اطلاعیه‌ها، گفتگوهای مجاز، گارانتی و پروفایل.
- `counterparty`: طرف حساب. به فیش‌هایی که توسط بازرگانی به او اختصاص داده شده دسترسی دارد و می‌تواند تایید، عودت یا رد کند.
- `finance`: کارمند مالی. بررسی و ثبت مالی فیش‌ها، عودت به بازرگانی، مشاهده سوابق و عملیات مالی.
- `finance_manager`: مدیر مالی. دسترسی‌های مالی گسترده‌تر، تایید نهایی، تفویض تایید، مدیریت بخشی از دسترسی‌ها و امکان ورود کد تفصیلی در صورت فعال بودن تنظیم.
- `commercial`: کارمند بازرگانی. ثبت بازرگانی، ثبت موقت، عودت به مالی، رد، ناقص، پیگیری و تخصیص طرف حساب.
- `commercial_manager`: مدیر بازرگانی. دسترسی‌های بازرگانی گسترده‌تر، مدیریت برخی عملیات، ورود کد تفصیلی در صورت فعال بودن تنظیم.
- `sales`: کارمند فروش. مشاهده مشتریان تخصیص‌یافته، سفارش‌ها، برنامه‌های واریز مرتبط و اطلاعات قابل دسترس.
- `sales_manager`: مدیر فروش. مدیریت تخصیص مشتری به فروش، سفارش‌ها، برنامه‌ها و دسترسی‌های فروش.
- `data_entry`: کاربر تکمیل اطلاعات فیش.
- `staff`: نقش عمومی کارمندی.
- `warranty`: کارشناس گارانتی.
- `warranty_manager`: مدیر گارانتی.
- `superuser`: دسترسی کامل مدیریتی.

### قواعد عمومی دسترسی

- کاربر `superuser` تقریبا به تمام بخش‌ها دسترسی دارد.
- هر کاربری که `is_staff` باشد یا نقش او در نقش‌های کارمندی تعریف شده باشد، کارمند سیستم محسوب می‌شود.
- کارمند فروش عادی فقط به مشتریانی دسترسی دارد که در `CustomerSalesAssignment` به او اختصاص داده شده‌اند.
- مشتری فقط اسناد، فاکتورها، سفارش‌ها، گفتگوها و اطلاعیه‌های مربوط به خودش را می‌بیند.
- طرف حساب فقط فیش‌هایی را می‌بیند که در فیلد `counterparty` همان فیش به او اختصاص داده شده است.
- مدیران بخش‌ها در صفحه مدیریت دسترسی، فقط کاربران همان بخش را مدیریت می‌کنند؛ `superuser` محدودیت بخشی ندارد.

## 3. مدل‌های داده اصلی

### کاربران و پروفایل

مدل `UserProfile` اطلاعات تکمیلی کاربر را نگهداری می‌کند:

- نام، نام خانوادگی، تلفن، موبایل، موبایل دوم
- نام مجموعه، شهر، استان، آدرس‌ها
- نقش کاربر
- تاریخ شروع فعالیت و اعتبار
- وضعیت تعلیق
- اجبار تغییر رمز
- دسترسی‌های خاص مانند مشاهده/بارگذاری فاکتور، تکمیل اطلاعات فیش و مغایرت‌گیری
- کد تفصیلی حسابداری مشتری
- آواتار یا تصویر نمایه

اگر کاربر معلق شود یا اعتبار او تمام شود، دسترسی عملیاتی می‌تواند محدود شود.

### تنظیمات سیستم

مدل `SystemSettings` تنظیمات عمومی را نگهداری می‌کند:

- لوگوی سیستم
- تنظیمات پیامک و MFA
- فعال بودن قابلیت ورود کد تفصیلی مشتریان از اکسل
- فعال بودن منوی گارانتی برای مشتریان
- تنظیمات جلسه کاربری و چندنشستی
- تنظیمات Jitsi برای تماس تصویری

مدل `UploadSettings` سقف حجم فایل‌های فیش و فاکتور را نگهداری می‌کند.

### تنظیم اجباری بودن فیلدها

مدل `FieldRequirementConfig` اجازه می‌دهد مدیر سیستم برای فرم‌های مختلف اجباری بودن فیلدها را تغییر دهد. منطق آن:

- اگر `is_required` خالی باشد، پیش‌فرض کد اعمال می‌شود.
- اگر `is_required=True` باشد، فیلد اجباری می‌شود.
- اگر `is_required=False` باشد، فیلد اختیاری می‌شود.
- تنظیمات 60 ثانیه cache می‌شوند.

فرم‌های تحت پوشش:

- ثبت فیش مشتری
- تکمیل اطلاعات فیش توسط کارمند
- ثبت سفارش
- اقلام سفارش
- پروفایل مشتری
- پیش‌فاکتور
- طرف حساب
- حساب بانکی طرف حساب

## 4. چرخه کامل فیش واریزی

مدل اصلی این بخش `PaymentRecord` است. هر فیش متعلق به یک مشتری است و می‌تواند چند فایل ضمیمه در `PaymentReceipt` داشته باشد.

### 4.1 ثبت فیش توسط مشتری

مشتری بر اساس بدهی، برنامه واریز یا اعلام تلفنی واحد فروش/بازرگانی، مبلغ را به حساب اعلام‌شده واریز می‌کند و فیش را در سامانه بارگذاری می‌کند.

رفتار ثبت:

- وضعیت اصلی سند روی `pending` یا «در حال بررسی» قرار می‌گیرد.
- فلگ مالی در حالت پیش‌فرض `None` است که یعنی «در انتظار ثبت مالی».
- تاریخچه عملیات در `PaymentActivityLog` ثبت می‌شود.
- نوتیفیکیشن برای کارکنان صادر می‌شود.
- رنگ نوتیفیکیشن ثبت اولیه فیش: `#DDF6D2`.
- پیامک ثبت فیش از نظر ساختار در سیستم پیش‌بینی شده، اما فعال بودن ارسال وابسته به تنظیمات پیامک است.

اطلاعات پیامک باید خلاصه باشد تا هزینه پیامک زیاد نشود. پیشنهاد متن عملیاتی:

```text
فیش شما ثبت شد.
مبلغ: ... ریال
کد پیگیری: ...
وضعیت: در حال بررسی
```

### 4.2 وضعیت‌های اصلی فیش

| مقدار فنی | عنوان نمایشی | معنی عملیاتی |
| --- | --- | --- |
| `pending` | در حال بررسی | فیش تازه ثبت شده و نیازمند بررسی است. |
| `commercial_review` | بررسی بازرگانی | سند در مرحله بررسی بازرگانی است. |
| `temp_commercial` | ثبت موقت بازرگانی | بازرگانی سند را موقتا ثبت کرده یا نیازمند کنترل بیشتر می‌داند. |
| `approved` | ثبت بازرگانی | سند توسط بازرگانی ثبت شده است. |
| `final_approved` | تایید نهایی | سند تایید نهایی شده است. |
| `rejected` | رد شده | سند از روال خارج شده و دیگر عملیاتی نیست. |
| `incomplete` | ناقص | فقط مشتری بر اساس توضیح کارشناس می‌تواند سند را اصلاح کند. |
| `returned_commercial` | عودت به بازرگانی | مالی سند را برای بررسی مجدد به بازرگانی عودت داده است. |
| `returned_finance` | عودت به مالی | بازرگانی سند را برای بررسی مجدد به مالی عودت داده است. |
| `follow_up` | پیگیری | سند نیازمند پیگیری مغایرت، صورتحساب یا بررسی بیرونی است. |

### 4.3 فلگ مالی

فلگ مالی مستقل از وضعیت اصلی/بازرگانی است:

| مقدار فنی | عنوان |
| --- | --- |
| `None` | در انتظار ثبت مالی |
| `finance_ok` | ثبت مالی |

وقتی مالی سند را ثبت کند:

- `finance_status='finance_ok'` می‌شود.
- `finance_registered_at` و `finance_registered_by` تکمیل می‌شوند.
- نوتیفیکیشن ثبت مالی با رنگ `#DDF6D2` صادر می‌شود.
- سند از داشبورد عملیاتی مالی خارج و در سوابق قابل مشاهده می‌شود، مگر اینکه وضعیت بعدی دوباره نیازمند اقدام کند.

### 4.4 عملیات بازرگانی

کارکنان بازرگانی می‌توانند همزمان با مالی روی سند اقدام کنند.

اقدام‌های اصلی:

- ثبت بازرگانی: وضعیت به `approved` می‌رود، فلگ بازرگانی «ثبت بازرگانی» محسوب می‌شود، رنگ نوتیفیکیشن `#B5F1CC`.
- ثبت موقت بازرگانی: وضعیت به `temp_commercial` می‌رود، رنگ نوتیفیکیشن `#FEEAC9`.
- عودت به مالی: وضعیت به `returned_finance` می‌رود و سند دوباره در داشبورد واحدهای مرتبط دیده می‌شود، رنگ نوتیفیکیشن `#DBEAFE`.
- رد سند: وضعیت به `rejected` می‌رود، سند قفل می‌شود و از روال عملیاتی خارج می‌شود، رنگ نوتیفیکیشن `#FECACA`.
- ناقص کردن سند: وضعیت به `incomplete` می‌رود؛ عملیات برای واحدهای شرکت بسته می‌شود و فقط مشتری باید سند را اصلاح کند.
- پیگیری: وضعیت به `follow_up` می‌رود؛ برای مواردی مانند برگشت وجه، مغایرت مبلغ، مشکل بانکی یا نیاز به صورتحساب.

هر اقدام باید در تاریخچه ثبت شود و در صورت نیاز توضیح کارشناس را ذخیره کند.

### 4.5 عملیات مالی

مالی می‌تواند:

- فیش را ثبت مالی کند.
- در صورت نیاز سند را به بازرگانی عودت دهد.
- در مسیر تایید نهایی، سند را تایید یا به صف تایید نهایی منتقل کند.

عودت به بازرگانی:

- وضعیت به `returned_commercial` تغییر می‌کند.
- سند دوباره در داشبورد قابل مشاهده می‌شود.
- کارکنان بازرگانی باید بتوانند آن را ببینند و روی آن اقدام کنند.
- رنگ پیشنهادی/پیاده‌سازی‌شده برای این نوتیفیکیشن: `#E9D5FF`.

### 4.6 طرف حساب

بازرگانی می‌تواند برای هر فیش یک طرف حساب تعیین کند.

رفتار طرف حساب:

- فیش در پنل طرف حساب قابل مشاهده می‌شود.
- طرف حساب می‌تواند پرداخت را تایید کند.
- طرف حساب می‌تواند پرداخت را عودت دهد یا رد کند و توضیح بنویسد.
- این عملیات نوتیفیکیشن ایجاد می‌کند.
- تایید طرف حساب با رنگ `#B5F1CC`.
- عودت/پیگیری طرف حساب با رنگ `#FEEAC9`.
- رد طرف حساب با رنگ `#FECACA`.

اگر طرف حساب پرداخت را تایید کند، بازرگانی می‌تواند سند را تایید کند یا در همان وضعیت نگه دارد. اگر طرف حساب تایید نکند، بازرگانی می‌تواند سند را به پیگیری ببرد و از مشتری صورتحساب یا توضیح بخواهد.

### 4.7 نمایش وضعیت برای مشتری

برای ساده‌سازی تجربه مشتری، بسیاری از وضعیت‌های داخلی به مشتری با عنوان «در حال بررسی» نمایش داده می‌شوند:

- `pending`
- `commercial_review`
- `temp_commercial`
- `returned_commercial`
- `returned_finance`
- `follow_up`

وضعیت‌های قابل نمایش مستقیم برای مشتری:

- `approved`: ثبت بازرگانی
- `final_approved`: تایید نهایی
- `rejected`: رد شده
- `incomplete`: ناقص

### 4.8 داشبورد و سوابق

قاعده کلی:

- داشبورد باید محل اسناد نیازمند اقدام باشد.
- اسنادی که عملیات آنها انجام شده است باید به سوابق منتقل شوند.
- اسناد ثبت مالی، ثبت بازرگانی، تایید نهایی، رد شده و موارد بسته‌شده نباید بی‌دلیل در داشبورد عملیاتی باقی بمانند.
- اسنادی که عودت می‌شوند یا نیازمند پیگیری هستند دوباره باید در داشبورد واحدهای مرتبط ظاهر شوند.
- بخش سوابق باید همه اسناد را با هر وضعیتی نشان دهد، مگر اینکه محدودیت دسترسی کاربر اجازه مشاهده ندهد.

## 5. نوتیفیکیشن‌ها

مدل `UserNotification` برای اعلان‌های سیستمی استفاده می‌شود.

فیلدهای مهم:

- کاربر گیرنده
- عنوان و متن
- لینک مقصد
- دسته‌بندی
- رنگ
- خوانده‌شده/خوانده‌نشده
- زمان خواندن

رفتار مورد انتظار:

- نوتیفیکیشن با رخدادهای مهم صادر شود.
- شمارنده فقط اعلان‌های خوانده‌نشده را نشان دهد.
- کلیک روی اعلان از مسیر `notifications/<id>/open/` باید اعلان را خوانده کند و سپس سند مقصد را باز کند.
- API خواندن گروهی از مسیر `api/notifications/read/` پشتیبانی می‌شود.
- بعد از خوانده شدن، عدد اعلان باید کاهش یابد.

### رنگ‌های مرجع نوتیفیکیشن

| رخداد | رنگ |
| --- | --- |
| ثبت فیش مشتری | `#DDF6D2` |
| ثبت مالی | `#DDF6D2` |
| ثبت بازرگانی | `#B5F1CC` |
| ثبت موقت بازرگانی | `#FEEAC9` |
| تایید طرف حساب | `#B5F1CC` |
| عودت/پیگیری طرف حساب | `#FEEAC9` |
| رد طرف حساب یا رد سند | `#FECACA` |
| عودت به بازرگانی | `#E9D5FF` |
| عودت به مالی | `#DBEAFE` |

### دسته‌بندی پیشنهادی اعلان‌ها

- فیش‌ها و اسناد مالی
- عودت‌ها و موارد نیازمند اقدام
- طرف حساب
- سفارش‌ها و پیش‌فاکتورها
- فاکتورها و لیست قیمت
- مغایرت‌گیری و گفتگوها
- گارانتی
- سیستم و دسترسی‌ها

## 6. فایل‌ها، پیش‌نمایش و دانلود

سیستم برای فایل‌های مختلف مسیر ذخیره یکتا تولید می‌کند. نام فایل روی سرور با الگوی امن و یکتا ذخیره می‌شود و نباید برای اهداف نمایشی تغییر کند.

نمونه پوشه‌ها:

- `receipts/`
- `invoices/`
- `price_lists/`
- `proformas/`
- `reconciliation_attachments/`
- `warranty/`
- `login_ads/`
- `profile_avatars/`
- `system_branding/`

### قواعد دانلود

نام فایل دانلودی باید کاربرپسند باشد و شامل اطلاعات سند شود. برای فیش‌ها، تصویرها و PDFها باید یک منطق واحد استفاده شود:

- اطلاعات قبلی سند
- نام صاحب حساب مقصد
- جداسازی با علامت `-`
- حفظ پسوند اصلی فایل

این قاعده فقط برای نام فایل دانلودی است و نام فایل روی سرور را تغییر نمی‌دهد.

### پیش‌نمایش PDF

برای PDF باید فقط خود سند نمایش داده شود و navigation panel مرورگر مخفی باشد. تنظیم نمایش PDF با پارامترهایی مانند زیر انجام می‌شود:

```text
#toolbar=1&navpanes=0&scrollbar=1&view=FitH
```

هدف:

- نمایش خود سند
- امکان بالا/پایین کردن
- امکان بزرگ‌نمایی و کوچک‌نمایی با ابزار مرورگر
- حذف پنل کناری صفحات

## 7. برنامه واریز و اطلاعیه روزانه فیش‌ها

مدل‌های اصلی:

- `DailyPaymentPlan`
- `DailyPaymentAssignment`
- `DailyPaymentNotice`

کارکرد:

- شرکت برای تاریخ مشخص، برنامه واریز تعریف می‌کند.
- مشتریان و کارمند فروش مرتبط به برنامه واریز اختصاص داده می‌شوند.
- هر کارمند فروش باید بداند چه مبلغی باید از مشتریان خود وصول کند.
- برای هر مشتری می‌توان گزارش تعداد فیش‌های ثبت‌شده و جمع مبلغ فیش‌های روز مشخص را آماده کرد.
- کارمند شرکت می‌تواند متن اطلاعیه را ویرایش کند.
- با انتشار، اطلاعیه در پرتال مشتری نمایش داده می‌شود.

قواعد:

- تاریخ پیش‌فرض برای ساخت گزارش، روز گذشته است.
- برای هر مشتری در هر تاریخ، انتشار تکراری باید هشدار دهد.
- اگر کارمند تایید کند، اطلاعیه قبلی ویرایش و دوباره برای مشتری نمایش داده می‌شود.
- مشتری پس از ورود باید اطلاعیه را یک‌بار برجسته ببیند.
- در refresh یا بازگشت به داشبورد، splash screen نباید مکرر نمایش داده شود.
- سوابق اطلاعیه‌ها برای مشتری و کارمند باید در جدول قابل مشاهده باشد.

## 8. مشتریان و کد تفصیلی

هر مشتری می‌تواند کد تفصیلی حسابداری داشته باشد. این مقدار در `UserProfile.accounting_code` ذخیره می‌شود.

ورود کد تفصیلی از اکسل:

- مسیر: `customers/import-accounting-codes/`
- نیازمند فعال بودن تنظیم `accounting_code_import_enabled`.
- دسترسی مجاز: `superuser`، `finance_manager` و `commercial_manager`.
- به دلیل یکسان نبودن قطعی اطلاعات اکسل با بانک اطلاعاتی، باید ورود داده همراه با تطبیق، پیش‌نمایش، صحت‌سنجی و گزارش موارد نامطمئن انجام شود.

اطلاعات مشتری در لیست‌ها بهتر است فقط نام کاربری نباشد و شامل موارد زیر شود:

- نام و نام خانوادگی
- نام مجموعه
- شهر و استان
- شماره تماس
- کد تفصیلی
- وضعیت فعال/معلق
- کارشناس فروش تخصیص‌یافته

## 9. فاکتورها

مدل‌های اصلی:

- `InvoiceRecord`
- `InvoiceExtractionJob`

امکانات:

- بارگذاری فاکتور توسط کاربران مجاز
- مشاهده فاکتور توسط مشتری یا کارمند مجاز
- پیش‌نمایش استخراج اطلاعات فاکتور
- ثبت یادداشت مشتری
- حذف فاکتور توسط کاربر مجاز
- دانلود فایل با نام مناسب

دسترسی:

- مشتری فاکتورهای خودش را می‌بیند.
- فروش، مدیر فروش، مدیر بازرگانی، مدیر مالی و کاربرانی که دسترسی فاکتور دارند می‌توانند بسته به نقش مشاهده کنند.
- فروش عادی فقط فاکتور مشتریان تخصیص‌یافته را می‌بیند.

## 10. لیست قیمت

مدل اصلی: `PriceList`

امکانات:

- بارگذاری لیست قیمت عمومی یا اختصاصی
- مشاهده لیست قیمت توسط مشتریان مجاز
- دانلود فایل
- حذف توسط کاربر مجاز
- ثبت زمان مشاهده مشتری

دسترسی بارگذاری:

- `superuser`
- بازرگانی و مدیر بازرگانی
- فروش و مدیر فروش
- مالی و مدیر مالی

## 11. سفارش‌ها و پیش‌فاکتورها

مدل‌های اصلی:

- `CustomerOrder`
- `CustomerOrderItem`
- `CustomerOrderLog`
- `ProformaInvoice`
- `ProformaInvoiceLog`
- `ProductCatalog`

وضعیت‌های سفارش:

| مقدار فنی | عنوان |
| --- | --- |
| `submitted` | ثبت شده |
| `reviewing` | در حال بررسی |
| `proforma_sent` | پیش‌فاکتور ارسال شده |
| `completed` | تکمیل شده |
| `cancelled` | لغو شده |

رفتار:

- مشتری می‌تواند سفارش ثبت کند.
- واحد فروش/بازرگانی سفارش را بررسی و وضعیت را تغییر می‌دهد.
- پیش‌فاکتور توسط کاربران مجاز صادر می‌شود.
- مشتری می‌تواند پیش‌فاکتور را مشاهده و وضعیت آن را تایید کند.
- تاریخچه سفارش و پیش‌فاکتور باید به‌روزرسانی شود.

دسترسی صدور پیش‌فاکتور:

- `superuser`
- فروش و مدیر فروش
- بازرگانی و مدیر بازرگانی

## 12. مغایرت‌گیری و گفتگوها

مدل‌های اصلی:

- `ReconciliationThread`
- `ReconciliationMessage`
- `ReconciliationMessageLog`
- `ReconciliationMessageReadReceipt`
- `ReconciliationReadState`
- `ReconciliationThreadPin`

انواع سند قابل اتصال:

- فیش پرداخت
- سفارش
- پیش‌فاکتور
- فاکتور
- برنامه واریز
- سایر

رفتار:

- همه کارکنان و مشتریان مجاز می‌توانند به مرکز مغایرت‌گیری دسترسی داشته باشند.
- مشتری فقط گفتگوهای خودش را می‌بیند.
- گفتگوهای داخلی برای مشتریان مخفی است.
- پیام‌ها می‌توانند متن، پیوست و ارجاع به سند داشته باشند.
- فایل‌های خطرناک مانند فایل اجرایی، اسکریپت، نصب‌کننده و مشابه در پیوست گفتگو مجاز نیستند.
- حداکثر حجم پیوست گفتگو 10 مگابایت است.
- وضعیت گفتگو می‌تواند باز یا بسته باشد.
- خوانده‌شدن گفتگو باعث کاهش شمارنده پیام‌های نخوانده می‌شود.

## 13. گارانتی و خدمات پس از فروش

مدل‌های اصلی:

- `WarrantyClaim`
- `WarrantyClaimFile`
- `WarrantyClaimLog`

وضعیت‌ها:

| مقدار فنی | عنوان |
| --- | --- |
| `submitted` | ثبت شده |
| `reviewing` | در حال بررسی |
| `info_needed` | نیاز به اطلاعات تکمیلی |
| `approved` | تایید گارانتی |
| `in_progress` | در حال پردازش |
| `resolved` | رفع شد |
| `rejected` | رد شده |
| `closed` | بسته شده |

اولویت‌ها:

- کم
- معمولی
- زیاد
- فوری

نوع نتیجه:

- تعمیر
- تعویض
- استرداد وجه

رفتار:

- مشتری، نماینده یا کارمند می‌تواند درخواست گارانتی ثبت کند.
- برای درخواست، کد پیگیری ساخته می‌شود.
- فایل‌های گارانتی ذخیره می‌شوند.
- کارشناس گارانتی می‌تواند بررسی را شروع کند، اطلاعات تکمیلی بخواهد، تایید کند، به پردازش ببرد، رفع کند، رد کند، ببندد، اولویت دهد یا تخصیص دهد.
- مشتری در وضعیت نیاز به اطلاعات تکمیلی می‌تواند پاسخ و فایل ارسال کند.
- پس از رفع یا بسته‌شدن، مشتری می‌تواند امتیاز و بازخورد ثبت کند.
- لاگ‌ها می‌توانند قابل مشاهده یا مخفی از مشتری باشند.

## 14. درخواست نمایندگی

مدل‌های اصلی:

- `AgencyApplication`
- `AgencyApplicationLog`

مراحل عمومی:

- ثبت شماره موبایل
- تایید موبایل
- تکمیل فرم درخواست
- دریافت کد پیگیری
- پیگیری وضعیت

وضعیت‌ها:

| مقدار فنی | عنوان |
| --- | --- |
| `pending` | در انتظار بررسی |
| `reviewing` | در حال بررسی |
| `info_needed` | نیاز به اطلاعات تکمیلی |
| `approved` | تایید شده |
| `rejected` | رد شده |

کارکنان فروش/مدیر فروش می‌توانند درخواست‌ها را بررسی و روی آنها اقدام کنند. در صورت تایید، امکان ایجاد کاربر مرتبط وجود دارد.

## 15. پیامک، OTP و MFA

سیستم دارای زیرساخت پیامک است:

- مدل‌های `SMSOTPCode` و `SMSSendLog`
- تنظیمات provider، کلید API، فرستنده، URL عمومی و تنظیمات اضافی
- ارسال OTP برای تایید هویت
- MFA پیامکی
- ارسال تست پیامک

نکته مهم:

- زیرساخت ارسال پیامک در سیستم وجود دارد، اما فعال یا غیرفعال بودن ارسال واقعی وابسته به تنظیمات `SystemSettings` است.
- پیامک‌های عملیاتی باید کوتاه باشند.
- در صورت فعال نبودن provider یا تنظیمات، سیستم نباید روال اصلی ثبت سند را متوقف کند.

## 16. مدیریت کاربران و دسترسی‌ها

بخش‌های مرتبط:

- لیست کاربران
- ویرایش کاربر
- کارت کاربری
- بازنشانی رمز عبور
- مدیریت دسترسی‌ها
- تنظیم نقش و پرچم‌های دسترسی

قواعد:

- فقط `superuser` مدیریت کامل کاربران را دارد.
- مدیران بخش‌ها می‌توانند دسترسی کاربران همان بخش را مدیریت کنند.
- مشتری و طرف حساب از مدیریت دسترسی کارکنان خارج هستند.
- تعلیق پروفایل یا غیرفعال‌کردن کاربر باید ورود و عملیات را محدود کند.

## 17. طرف حساب‌ها و حساب‌های بانکی

مدل‌های اصلی:

- `Counterparty`
- `CounterpartyBankAccount`

وضعیت طرف حساب:

| مقدار فنی | عنوان |
| --- | --- |
| `active` | فعال؛ ورود و عملیات مجاز |
| `inactive` | غیرفعال؛ ورود مجاز، عملیات ممنوع |
| `suspended` | معلق؛ ورود ممنوع |

قواعد:

- اگر طرف حساب معلق شود، کاربر مرتبط غیرفعال می‌شود.
- هر طرف حساب می‌تواند چند حساب بانکی داشته باشد.
- فقط یک حساب بانکی می‌تواند اصلی باشد.
- حساب‌های طرف حساب در انتخاب حساب مقصد و طرف حساب برای فیش کاربرد دارند.

## 18. تجربه کاربری وب و موبایل

سیستم باید دو تجربه هم‌راستا داشته باشد:

- محیط وب دسکتاپ
- محیط موبایلی/PWA

قواعد طراحی فعلی:

- فونت اصلی باید «وزیر» باشد.
- رابط راست‌چین است.
- منوهای دسکتاپ در سایدبار قابل استفاده هستند.
- در موبایل، منوی پایین و منوی سه‌خط برای دسترسی سریع استفاده می‌شود.
- دکمه خروج باید در سایدبار/منوی سه‌خط موبایل موجود باشد.
- کپشن منوها باید خوانا و دارای کنتراست کافی باشند.
- در دسکتاپ، لوگو و عبارت «سیستم مدیریت مشتریان» در سمت راست قرار می‌گیرند و ابزارهای کاربر/پیام/اعلان در سمت چپ.
- کارت‌ها نباید باعث مخفی شدن دکمه‌ها یا نیاز به اسکرول افقی شوند.
- فرم‌ها باید فیلدهای اجباری را با ستاره قرمز نشان دهند.
- فیلدهای حساب مبدا و حساب مقصد باید خوانا و جدا از هم باشند.
- در موبایل، اطلاعات جدولی باید به کارت یا layout قابل خواندن تبدیل شود.

PWA:

- مسیر manifest: `manifest.json`
- مسیر service worker: `sw.js`
- نسخه cache فعلی در کد: `rabasa-customer-pwa-v14`
- فایل‌های CSS/JS با نسخه `ui-redesign-v14` cache-bust می‌شوند.

## 19. امنیت و اعتبارسنجی فایل‌ها

قواعد عمومی:

- نام فایل روی سرور یکتا و امن ساخته می‌شود.
- پسوند فایل پاک‌سازی می‌شود.
- برای تصویرها، در صورت نیاز resize و optimize انجام می‌شود.
- فایل‌های گفتگو با پسوندهای خطرناک مسدود می‌شوند.
- حجم فایل‌ها بر اساس تنظیمات سیستم کنترل می‌شود.
- کاربر بدون دسترسی به سند نباید فایل آن را ببیند یا دانلود کند.

## 20. مسیرهای اصلی سیستم

### مشتری و فیش

- `submit/`: داشبورد/ثبت فیش
- `submit/new/`: ثبت فیش جدید
- `success/`: نتیجه ثبت
- `payments/<id>/timeline/`: تاریخچه و جزئیات سند
- `payments/history/`: سوابق فیش‌ها
- `receipts/<id>/file/`: مشاهده یا دانلود فایل فیش
- `receipts/<id>/rotate/`: چرخش تصویر فیش

### نوتیفیکیشن

- `api/notifications/`
- `api/notifications/read/`
- `notifications/<id>/open/`

### مشتریان و برنامه واریز

- `customers/`
- `customer/<id>/`
- `customers/import-accounting-codes/`
- `daily-payments/`
- `daily-payments/<id>/`
- `daily-payment-notices/`
- `daily-payment-notices/<id>/seen/`

### مالی و تایید

- `payments/<id>/finance-register/`
- `payments/<id>/finance-action/`
- `payments/<id>/final-approve/`
- `finance/pending-final-approval/`
- `finance/delegation/`
- `finance/bulk-approve/`

### بازرگانی و وضعیت سند

- `payments/<id>/status/`
- `payments/<id>/details-edit/`
- `payments/<id>/edit/`
- `payments/<id>/note/`

### مغایرت‌گیری

- `finance/reconciliation/`
- `finance/reconciliation/poll/`
- `finance/reconciliation/start-thread/`
- `finance/reconciliation/attachments/<message_id>/`
- `api/reconciliation-messages/`
- `call/`

### فاکتور، لیست قیمت، سفارش، پیش‌فاکتور

- `invoices/`
- `invoices/parse-preview/`
- `invoices/<id>/`
- `invoices/<id>/file/`
- `price-lists/`
- `price-lists/<id>/file/`
- `orders/`
- `orders/<id>/`
- `proformas/`
- `proformas/<id>/`
- `proformas/<id>/file/`

### طرف حساب

- `counterparty/`
- `counterparty/payments/<id>/approve/`
- `counterparty/payments/<id>/return-cp/`
- `counterparty/payments/<id>/reject-cp/`
- `counterparties/`
- `counterparties/manage/`

### گارانتی

- `warranty/`
- `warranty/my/`
- `warranty/track/`
- `warranty/<id>/`
- `warranty/staff/`
- `warranty/staff/<id>/`

### نمایندگی

- `agency/`
- `agency/verify/`
- `agency/apply/`
- `agency/track/`
- `sales/agency/`

### مدیریت و ابزارها

- `users/`
- `access-management/`
- `export-records/`
- `export/<dataset>/`
- `admin-tools/receipt-reader/`
- `admin-tools/system-logo/`
- `sms-verify/`
- `sms-mfa/setup/`
- `profile/`

## 21. گزارش‌گیری و خروجی‌ها

سیستم مسیرهای خروجی داده دارد:

- `export-records/`
- `export/<dataset>/`

خروجی‌ها برای گزارش‌گیری مدیریتی، سوابق اسناد، مشتریان، فیش‌ها یا داده‌های تعریف‌شده استفاده می‌شوند. هر خروجی باید دسترسی کاربر را رعایت کند.

## 22. تاریخچه و لاگ‌ها

مدل‌های لاگ مهم:

- `PaymentActivityLog`
- `SystemActivityLog`
- `CustomerOrderLog`
- `ProformaInvoiceLog`
- `ReconciliationMessageLog`
- `WarrantyClaimLog`
- `AgencyApplicationLog`
- `SMSSendLog`
- `LoginRecord`

قاعده:

- هر تغییر وضعیت مهم باید لاگ شود.
- لاگ باید actor، زمان، action و توضیح لازم را نگهداری کند.
- لاگ‌های مشتری‌محور باید مشخص کنند کدام موارد برای مشتری قابل مشاهده است.

## 23. مواردی که باید هنگام توسعه رعایت شود

- هر migration مرتبط با مدل‌ها باید اجرا شود؛ نبود ستون جدید باعث خطای `ProgrammingError` می‌شود.
- بعد از تغییر مدل، `makemigrations`، `migrate` و `check` باید اجرا شود.
- بعد از تغییر فایل‌های static، cache version و در صورت نیاز `collectstatic` باید هماهنگ شود.
- تغییر UI موبایل نباید دسکتاپ را خراب کند و برعکس.
- قبل از نهایی‌کردن تغییرات فیش، سناریوهای مشتری، مالی، بازرگانی، فروش و طرف حساب باید تست شوند.
- تاریخچه و نوتیفیکیشن در هر تغییر وضعیت باید بررسی شود.
- اگر فیلدی از سمت ادمین اختیاری شده، فرم نباید کاربر را مجبور به ورود کند.
- اگر فیلدی از سمت ادمین اجباری شده، باید با ستاره قرمز و پیام خطای واضح نمایش داده شود.

## 24. سناریوهای تست پیشنهادی

### فیش واریزی

1. مشتری فیش تصویر ثبت کند.
2. مشتری فیش PDF ثبت کند.
3. مشتری فیش با مبلغ و کد پیگیری غیرعددی/ترکیبی ثبت کند.
4. بازرگانی سند را ثبت بازرگانی کند.
5. بازرگانی سند را ثبت موقت کند.
6. بازرگانی سند را ناقص کند و مشتری آن را اصلاح کند.
7. بازرگانی سند را رد کند و مطمئن شود عملیاتی نیست.
8. مالی سند را ثبت مالی کند.
9. مالی سند را به بازرگانی عودت دهد.
10. بازرگانی سند را به مالی عودت دهد.
11. طرف حساب سند را تایید کند.
12. طرف حساب سند را رد یا عودت دهد.
13. هر مرحله از تاریخچه و نوتیفیکیشن بررسی شود.

### نوتیفیکیشن

1. اعلان جدید ایجاد شود.
2. شمارنده اعلان افزایش یابد.
3. با کلیک روی اعلان، سند باز شود.
4. اعلان خوانده شود.
5. شمارنده کاهش یابد.
6. خواندن گروهی اعلان‌ها تست شود.

### فایل

1. پیش‌نمایش JPG در دسکتاپ و موبایل.
2. پیش‌نمایش PDF در دسکتاپ و موبایل.
3. دانلود JPG با نام استاندارد.
4. دانلود PDF با نام استاندارد.
5. عدم دسترسی کاربر غیرمجاز به فایل.

### UI وب و موبایل

1. داشبورد مشتری در موبایل بدون اسکرول افقی.
2. داشبورد کارکنان در موبایل بدون بهم‌ریختگی کارت‌ها.
3. منوی سه‌خط و دکمه خروج در موبایل.
4. سایدبار دسکتاپ و کپشن منوها.
5. فرم ثبت فیش و فرم تکمیل اطلاعات فیش.
6. لیست مشتریان، جزئیات مشتری و دکمه‌ها بدون نیاز به اسکرول آزاردهنده.

### برنامه واریز و اطلاعیه روزانه

1. تاریخ پیش‌فرض روز گذشته باشد.
2. گزارش روزانه برای مشتری ساخته شود.
3. انتشار تکراری برای همان مشتری/تاریخ هشدار دهد.
4. با تایید کارمند، اطلاعیه قبلی ویرایش و دوباره نمایش داده شود.
5. splash فقط یک‌بار در ورود نمایش داده شود.
6. سوابق مشتری و کارمند در جدول قابل مشاهده باشد.

## 25. وضعیت‌های تصمیم‌گیری مهم

این موارد در سیستم پیاده‌سازی یا طراحی شده‌اند و هنگام تغییرات بعدی باید حفظ شوند:

- فلگ مالی مستقل از وضعیت بازرگانی است.
- ثبت مالی و ثبت بازرگانی می‌توانند مستقل و همزمان پیش بروند.
- عودت به مالی/بازرگانی باید سند را دوباره عملیاتی و قابل مشاهده در داشبورد کند.
- رد سند، سند را از روال عملیاتی خارج می‌کند.
- ناقص شدن سند، عملیات کارمندی را متوقف و اصلاح را به مشتری منتقل می‌کند.
- همه اسناد باید در سوابق قابل پیگیری باشند.
- نوتیفیکیشن‌ها باید رنگ، دسته‌بندی و کاهش شمارنده بعد از بازدید داشته باشند.
- نام دانلود فایل‌ها باید برای تصویر و PDF یکسان و مبتنی بر اطلاعات سند باشد.
- طراحی موبایل و دسکتاپ باید هم‌خانواده باشد، اما layout آنها متناسب با اندازه صفحه تغییر کند.

